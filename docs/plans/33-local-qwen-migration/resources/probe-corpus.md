# Local Qwen capability probe corpus

These three short episodes are intentionally fixed. The probe harness reads them as
the same input for every effort level and every agent.

## E1 — plain factual

NeoCortex uses PostgreSQL for its memory graph. The extraction worker stores an
episode and links it to the PostgreSQL database.

## E2 — temporal correction

The migration was scheduled to land on 15 August. **Correction: it actually landed
on 14 August, not the 15th.** The release note records the corrected date.

## E3 — adversarial type bait

DishGreg was served at LocationSalCapeVerde during a technical demonstration of
DreamAiPresentation. The system used PostgreSQL event sourcing, a B47 engine, and
an API gateway. These are individual names and implementation details, not reusable
ontology types; prefer reusable types such as Dish, Location, Presentation, Vehicle,
Database, and SoftwareComponent.
